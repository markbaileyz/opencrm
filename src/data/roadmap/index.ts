
// This file exports all roadmap progress data for easy imports
import { completedFeatures } from './completed-features';
import { nextSteps } from './next-steps';
import { knownIssues } from './known-issues';
import { lastUpdated } from './last-updated';

export const roadmapProgress = {
  completedFeatures,
  nextSteps,
  knownIssues,
  lastUpdated
};

export { completedFeatures, nextSteps, knownIssues, lastUpdated };
